/* Compatibility layer for 9front includes */
#ifndef _COMPAT_H_
#define _COMPAT_H_

/* Map old relative paths to new flat structure */
#define __stringify(x) #x
#define __header(x) __stringify(x)

#endif /* _COMPAT_H_ */
