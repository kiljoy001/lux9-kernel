</$objtype/mkfile

TARG=cmdexec
OFILES=cmdexec.$O
HFILES=
BIN=$home/bin/$objtype

# Link with lib9p for 9P filesystem functions
LIBS=/sys/src/lib9p/lib9p.a$O

</sys/src/cmd/mkone

# Override linking to include lib9p
$O.out: $OFILES
	$LD -o $target $LDFLAGS $OFILES $LIBS

clean:V:
	rm -f *.$O $O.out *.acid $TARG $CLEANFILES

nuke:V: clean
	rm -f $BIN/$TARG